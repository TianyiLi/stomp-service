{
  "name": "stomp-service",
  "version": "1.0.3",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "rm -rf ./dist && tsc",
    "build-win": "rm -rf ./dist && tsc",
    "test": "mocha --compilers ts:ts-node/register ./test/*.spec.ts"
  },
  "license": "MIT",
  "dependencies": {
    "webstomp-client": "^1.2.0",
    "ws": "^4.0.0"
  },
  "devDependencies": {
    "@types/mocha": "^2.2.47",
    "@types/node": "^9.3.0",
    "@types/ws": "^4.0.0",
    "merge": "^1.2.0",
    "mocha": "^5.0.0"
  }
}
